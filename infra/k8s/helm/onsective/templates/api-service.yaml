apiVersion: v1
kind: Service
metadata:
  name: onsective-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: api
  ports:
    - name: http
      port: 80
      targetPort: {{ .Values.api.port }}
