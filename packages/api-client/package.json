{
  "name": "@onsective/api-client",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "echo skip",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist .turbo"
  },
  "dependencies": {
    "@onsective/shared-types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "rimraf": "^5.0.10"
  }
}
