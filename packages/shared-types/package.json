{
  "name": "@onsective/shared-types",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "echo skip",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist .turbo"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "rimraf": "^5.0.10"
  }
}
